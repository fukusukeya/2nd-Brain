# 2026-03-12 備忘録: 外部CMOに向けた「まちスタジオ」特化型WordPress自作テーマ構築

## 現在の進捗状況
ローカル環境にて「きみそら」のLPデザインをベースに、完全オリジナルのWordPressテーマ構築を開始。デザインとCSSの正常な反映（静的サイトの移植）まで完了しました。

### 完了したステップ（Step 1〜4）
1. `Local` アプリを使ったローカル環境（fukusukeya-studio）の立ち上げ
2. `wp-content/themes/` 内へのオリジナルテーマフォルダ（`fukusuke-theme`）の作成
3. テーマ認識の必須ファイル（`style.css`, `index.php`）の作成と有効化
4. HTMLの流し込みと、CSS/画像パスのWordPress対応（`<?php echo get_template_directory_uri(); ?>` への置換）完了

## 明日やること（Next Actions）
明日は以下の「Step 5: より実践的なWordPress化」から再開します。

### Step 5: WordPress関数の適用と動的化
1. **ファイルの分割:** `index.php` を `header.php`（上部）と `footer.php`（下部）に切り離し、共通パーツ化する。
2. **必須タグの埋め込み:** プラグインや管理画面の設定を動作させるための「魔法の関数（`wp_head();` と `wp_footer();`）」を埋め込む。
3. **管理バーの確認:** サイト上部にWordPress管理画面への黒いバーが表示されるか確認する。

### 起動時のプロンプト（AIへの指示）
明日作業を再開する際は、私（AI）に対して以下のようにお声がけください。

> 「昨日作った『fukusuke-theme』の続きをやります。Step 5のファイル分割（headerとfooter）からレクチャーをお願いします。」

---
*Memo: 「きみそら」のLP（1ページ構成）をベースに、どのように「実績」や「お知らせ」などの動的な要素を絡めていくか、システムとデザインの結合を試みています。ここをマスターすれば、保守・運用（ストック収益）の強力な武器になります。*
